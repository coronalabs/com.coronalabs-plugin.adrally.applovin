Sources for the plugin `plugin.adrally.applovin`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.adrally.applovin" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
